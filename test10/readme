HTML파일에 ul태크와 그 자식태그로 li태크 생성
CSS로 간단히 마크업 진행한뒤 클래스명들 자바스크립트에서 변수 선언
데이터를 구현하는 작업을 위해 임시로 dummy_data 생성
함수 내부에 반복문을 돌려서 태그가 지정되어 있는 변수에 innerHTML, style.height로 배열 요소값을 HTML태그값에 삽입.
리액트에서 컴포넌트에 데이터를 입히는 방식과 약간의 문법차이만 있을 뿐 본질은 다르지 않았음.